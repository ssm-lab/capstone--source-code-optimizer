from src.utils import Utility

def process_data(data):
    """
    Process some data and call the unused_member_method from Utility.
    """
    util = Utility()
    util.unused_member_method(data)
    return data.upper()
