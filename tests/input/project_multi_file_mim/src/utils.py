class Utility:
    def unused_member_method(self, param):
        """
        A method that accepts a parameter but doesn’t use it.
        This demonstrates the member ignoring code smell.
        """
        print("This method is defined but doesn’t use its parameter.")
